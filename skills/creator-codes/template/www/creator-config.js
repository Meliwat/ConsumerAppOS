/* Creator-codes client config. The codes list is PUBLIC static data —
   creators share their codes publicly by design; there is nothing secret
   in it or in this URL. Empty string = no fetch: entered codes are saved
   as pending and validated on a later launch once the URL exists.
   The slot is filled with a JSON-SERIALIZED string. */
window.APP_CODES_URL = {{CODES_URL_JSON}};
